import { Meta } from '@/layouts/Meta';
import { Main } from '@/templates/Main';

const Portfolio = () => {
  return (
    <Main meta={<Meta title="Duncan/Portfolio" description="My portfolio." />}>
      <h1>Portfolio Page!</h1>
    </Main>
  );
};

export default Portfolio;
